{# {% extends 'base.html.twig' %} #}

{% block title %}Patient{% endblock %}

{% block body %}
    <h1>Patient</h1>
{{dump(patient)}}
________________________
{{dump(user)}}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patient.id }}</td>
            </tr>
            <tr>
                <th>Family_history</th>
                <td>{{ patient.familyHistory }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ patient.profession }}</td>
            </tr>
            <tr>
                <th>Status_patient</th>
                <td>{{ patient.statusPatient }}</td>
            </tr>
            <tr>
                <th>Cree_en</th>
                {# <td>{{ patient.creeEn ? patient.creeEn|date('Y-m-d H:i:s') : '' }}</td> #}
            </tr>
            <tr>
                <th>Mise_a_jour_a</th>
                {# <td>{{ patient.miseAJourA ? patient.miseAJourA|date('Y-m-d H:i:s') : '' }}</td> #}
            </tr>
        </tbody>
    </table>
    {# show data hasDossiers #}

     
    <a href="{{ path('app_patient_index') }}">back to list</a>

    <a href="{{ path('app_patient_edit', {'id': patient.id}) }}">edit</a>

    {{ include('patient/_delete_form.html.twig') }}
{% endblock %}
