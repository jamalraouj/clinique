<form method="post" class="w-50" action="{{ path('app_medecin_delete', {'id': medecin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this doctor?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medecin.id) }}">
    <button class="btn btn-danger w-100">Delete</button>
</form>
