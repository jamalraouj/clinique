{{ form_start(form ,{ 'attr' : { 'class': 'w-50 medecinForm' } , 'enctype' : 'multipart/form-data' }) }}
    {{ form_widget(form) }}
    <button class="btn btn-success w-100 text-center my-3">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}